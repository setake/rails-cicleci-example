<%= form_with(model: testapp, local: true) do |form| %>
  <% if testapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testapp.errors.count, "error") %> prohibited this testapp from being saved:</h2>

      <ul>
      <% testapp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :testapp_name %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :testapp_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :testapp_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
